{
    "name": "paradise-plugin",
    "version": "1.0.0",
    "description": "A custom plugin for Paradise",
    "scripts": {
        "build": "clj -M:build",
        "serve": "npx serve dist -C --cors"
    }
}
